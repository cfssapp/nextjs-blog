import type { RequestHandler, Compiler } from '@umijs/bundler-webpack';
import { IApi } from '../../types';
declare function createRouteMiddleware(opts: {
    api: IApi;
}): ({ compiler }: {
    compiler: Compiler;
}) => RequestHandler;
export { createRouteMiddleware };
