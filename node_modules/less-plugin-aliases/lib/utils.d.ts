/**
 * @fileoverview the utils function for custom render
 * @author houquan | houquan@bytedance.com
 * @version 1.0.0 | 2020-03-16 | houquan      // initial version
 */
/// <reference types="less" />
import tsModule from 'typescript/lib/tsserverlibrary';
export interface Logger {
    log: (msg: string) => void;
    error: (error: Error) => void;
}
export interface InjectOptionsFromTSPlugin {
    fileName: string;
    compilerOptions: tsModule.CompilerOptions;
    logger: Logger;
}
export declare function normalizePath(filename: string): string | undefined;
export declare function transformPathsToAliase({ paths, baseUrl }?: tsModule.CompilerOptions): Record<string, string[]>;
export declare function readTSConf(): any;
export declare function createCustomRender(options: Less.Options & {
    prefix?: string;
}): (css: string, { fileName: filename, logger, compilerOptions }: InjectOptionsFromTSPlugin) => string;
